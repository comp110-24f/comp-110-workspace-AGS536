"""Practice with function"""

# Define the function
# def addNums(num1: int, num2: int) -> int:
#   """Adds two nums together"""
#   return num1 + num2


# Call the function
def total_price(price: int, tax_rate: float):
    print(price + (price * tax_rate))


print(total_price(price=100, tax_rate=0.07))


# def total_cost(price: int, tax_rate: float):
# return price + (price * tax_rate)


# print(total_cost(price=100, tax_rate=0.07))
